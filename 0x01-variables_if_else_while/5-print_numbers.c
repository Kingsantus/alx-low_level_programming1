#include <stdio.h>

/**
 * main - entry point for code
 *
 * Return: always 0 success
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	print('\n');
	return (0);
}
